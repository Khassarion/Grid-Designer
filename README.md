# Grid Designer

브라우저에서 이미지를 격자·중첩 레이아웃으로 배치하고 **PNG**로 내보내는 정적 웹 에디터입니다.  
서버·설치·빌드 없이 `index.html`만 열면 동작합니다.

**현재 버전:** `1.1.2.2` · 소스: [`js/version.js`](js/version.js)

**사용:** [https://khassarion.github.io/Grid-Designer](https://khassarion.github.io/Grid-Designer)

**튜토리얼 (후원 이미지 벽지):** [tutorial.html](tutorial.html) · [배포본](https://khassarion.github.io/Grid-Designer/tutorial.html)

---

## 변경 이력

버전은 `MAJOR.MINOR.PATCH.BUILD` 형식입니다.  
앱 릴리즈 버전(`js/version.js`)과 구조 JSON의 `version` 필드(`PROJECT_VERSION`)는 별개입니다.
### v1.1.2.2 - 2026-08-20
- 저장소 소유자 변경으로 사이트 주소가 변경되었습니다.


### v1.1.2.1 — 2026-08-16
- 버전 업데이트 시 **업데이트 안내** 창 표시 (이전에 본 버전은 브라우저에 저장)

### v1.1.2.0 — 2026-08-15
레이아웃 리사이즈·계층구조 저장 UI.

- **Ctrl(⌘)+레이아웃 크기 조절**: 드래그 중 CSS 스케일 미리보기, 마우스 업 시 레이아웃·셀·패딩·간격(px) 확정
- **Shift+크기 조절**: 이미지뿐 아니라 레이아웃도 비율 유지
- 앱바 버튼을 **📂 계층구조 열기** / **💾 계층구조 저장**으로 명확화

### v1.1.1.0 — 2026-08-15

레이아웃 보이기/숨기기, 자동 추가 다중 규칙.

- 계층 구조에서 레이아웃별 **눈 아이콘**으로 보이기/숨기기 
- 숨긴 레이아웃은 미리보기·PNG 내보내기에서 제외 (트리에는 유지, Undo·프로젝트 저장 지원)
- **자동 추가**: 감시 폴더 ↔ 대상 레이아웃 규칙을 여러 개 등록 (+/−), 설정은 프로젝트 JSON에도 저장

### v1.1.0 — 2026-08-14

미리보기·계층 구조 성능과 조작성을 다듬었습니다.

- 미리보기 드래그 시 해당 셀만 갱신 (전체 DOM 재생성 제거, 강제 레이아웃 비용 감소)
- 계층 구조 **보기 ▾** 메뉴: 썸네일 표시 토글, 모두 접기/펼치기
- 계층 스크롤 위치 유지 (재렌더·접기/펼치기 시 선택 항목으로 스크롤이 튀지 않음)
- 계층·설정·미리보기 스크롤바 숨김 (휠/트랙패드 스크롤은 유지)
- 계층 순서 = 렌더 순서(`children[]`, 아래가 앞). 미리보기 클릭으로 순서를 바꾸지 않음
- 이미지 포함 프로젝트 로드·복제 경로 추가 최적화 (`blob:` URL, refresh 합치기 등)

### v1.0.0 — 2026-08-14

첫 공식 버전 넘버링. 기존 웹 에디터 기능을 기준으로 정리했습니다.

- 제품명 **Grid Designer**로 통일, favicon·GitHub Pages 저장소명 정리
- 이미지 포함 구조 파일 로드·복제 성능 개선 (`data:` → `blob:` URL, UI refresh 합치기, 자동 내보내기 양보)
- 계층 구조 최적화 (썸네일 지연 로드, 자식이 많은 레이아웃 복제/붙여넣기 시 접기)
- 폴더 감시로 새 이미지 자동 추가 (Chrome/Edge)
- 한국어 / English UI
- 후원 이미지 벽지 튜토리얼 페이지
- 구조 JSON 저장·불러오기 (이미지 포함 옵션)
- PNG 내보내기·자동 내보내기 (저장 위치 고정)
- 캔버스·중첩 레이아웃, 복사/복제/실행 취소 등 편집 기능

---

## 사용법

위 페이지에서 바로 쓰거나, 저장소의 `index.html`을 브라우저로 열면 됩니다.
후원 이미지로 OBS 벽지를 만드는 순서는 [튜토리얼](tutorial.html)을 보세요.

앱바에서 **한국어 / English**를 선택할 수 있습니다 (기본: 한국어).

### 개념

| 대상 | 역할 |
|------|------|
| **캔버스** | 최상위 컨테이너. 격자 없음. 크기 = 내보내는 PNG 해상도 |
| **레이아웃** | 격자 배치 단위. 패딩·셀·스페이싱·시작 코너/축·자식 정렬·제약 설정 |

- 캔버스 아래에는 레이아웃·이미지를 자유롭게 둡니다.
- 레이아웃 안에는 이미지·하위 레이아웃을 격자로 둡니다.
- 하위 레이아웃(레이아웃의 자식)은 **크기 입력이 잠기고**, 격자 재배치 시 부모 **셀 크기**에 맞춰집니다. 내부는 스케일하지 않고 **1:1**로 그립니다.
- 레이아웃보다 큰 셀 그룹은 잘리지 않고 **오버플로**로 밖으로 나옵니다.

### 레이아웃 설정

| 항목 | 설명 | 기본 |
|------|------|------|
| 패딩 | 좌/우/상/하 | 0 |
| 셀 크기 | 고정 px. 이미지는 셀에 꽉 채움 (`object-fit: fill`) | 200×160 |
| 스페이싱 | 셀 간격 X/Y (음수 가능, 겹침) | 0 |
| 시작 코너 | Left/Right × Top/Bottom | Left Top |
| 시작 축 | 수평 / 수직 | 수평 |
| 자식 정렬 | 셀 **그룹**이 레이아웃 안 어디에 붙는지 (9점). 이미지→셀 정렬 아님 | Left Top |
| 제약 | 자동 / 열 개수 / 행 개수 | 자동 |
| 배경 | 색(RGB) + 불투명도(0–100). 0이면 완전 투명 | 투명 |

**크기 프리셋**

- 캔버스: `3840×2160` · `2560×1440` · `1920×1080`
- 레이아웃(캔버스 직하위): `200×1080` · `1920×160` · `400×1080`
- 부모 셀에 크기가 잠긴 하위 레이아웃에서는 프리셋 숨김

### 격자 재배치

다음일 때 레이아웃 자식이 격자에 다시 맞춰집니다.

- 자식 추가 / 제거 / 전체 삭제
- 하위 레이아웃 삭제(부모 레이아웃 쪽)
- 레이아웃 설정 변경 (패딩·셀·스페이싱·배치·정렬·제약 등)
- 레이아웃 **크기** 변경 (입력·프리셋·미리보기 리사이즈 — **드래그 중에도 실시간**)

### 미리보기 조작

| 대상 | 클릭·이동·리사이즈 |
|------|-------------------|
| 캔버스 직속 이미지 | 가능 |
| 모든 레이아웃 프레임 | 가능 |
| 레이아웃 안의 이미지 | 불가 |

- **자석**: 레이아웃 이동·크기 조절 시 다른 레이아웃 가장자리·부모 경계·중앙에 스냅 (기본 ON)
- **비율 유지**: 이미지·레이아웃 크기 조절 시 `Shift`를 누르면 비율 유지
- **Ctrl(⌘)+레이아웃 크기 조절**: 드래그 중에는 내용 전체를 CSS로 스케일 미리보기(격자 재배치 없음). 마우스를 떼면 레이아웃·셀·패딩·간격(px)을 비율에 맞게 확정
- 휠로 확대/축소, 미리보기 레이아웃에 이미지 드롭으로 추가
- 계층 구조에 레이아웃과 이미지가 함께 표시됩니다 (형제 간 드래그로 순서 변경). 순서 = 렌더 순서(아래가 앞)
- 레이아웃 옆 **눈 아이콘**으로 보이기/숨기기 (숨기면 미리보기·PNG에서 제외)
- **보기 ▾** — 썸네일 표시 토글, 레이아웃 모두 접기/펼치기
- 이미지는 `Ctrl+클릭`으로 다중 선택, `Shift+클릭`으로 같은 부모 아래 연속 선택 후 미리보기로 드래그해 일괄 이동
- **추가 ▾ → 자동 설정** — 감시 폴더별 대상 레이아웃을 여러 개 둘 수 있음. 새 이미지가 생기면 해당 레이아웃에 자동 추가 (Chrome/Edge, 탭이 열린 동안). 설정은 프로젝트 JSON에도 저장되며, 폴더 접근 권한은 브라우저에 유지(기본 OFF). 사용 중이면 추가 버튼에 작은 `auto` 표시, 메뉴에 ON 표시
- 레이아웃 이름 더블클릭으로 이름 변경
- 계층 구조 **추가** 메뉴: 이미지 추가 · 레이아웃 추가
- 선택 항목이 없으면 **제거** 비활성화
- `Delete`로 선택 항목 제거, `Ctrl+Z`로 실행 취소
- `Ctrl+C` / `Ctrl+V`로 레이아웃(하위 포함) 또는 이미지 복사·붙여넣기
- `Ctrl+D`로 선택 항목 바로 아래(형제)로 복제
- `Ctrl+S`로 PNG 내보내기, `Ctrl+Shift+S`로 구조 저장

### 구조 저장·불러오기

- **저장** — 캔버스·레이아웃·배치·자동 추가(감시 폴더↔레이아웃) 설정을 JSON으로 다운로드
- **이미지 포함** 체크 시 이미지도 JSON에 함께 저장 (기본 체크)
- **열기** — JSON으로 복원 (다른 브라우저·PC에서도 동일). 기존 작업은 덮어씀. 감시 폴더는 브라우저 권한이 필요하므로, 같은 PC/브라우저에서는 가능하면 이어서 쓰고, 아니면 자동 설정에서 다시 선택
- 이미지 없이 저장하면 레이아웃 구조만 남고, 이미지 슬롯은 저장되지 않습니다

### PNG 내보내기

- **내보내기**로 현재 캔버스를 PNG로 저장
- **자동 내보내기**를 켜면 구조 변경 시 PNG를 자동 저장 (PNG 위치가 지정된 경우에만, 미리보기 크기 조절·숫자 입력은 끝난 뒤에만). 기본 켜짐
- (지원 브라우저) 저장 위치를 고정해 두고 같은 파일에 덮어쓰기 가능

### 사용 흐름 예

1. 미리보기 상단에서 캔버스 해상도 설정
2. **+ 하위**로 레이아웃 추가 → 크기·셀·패딩 등 설정
3. 레이아웃에 이미지 추가 (또는 드롭)
4. 필요하면 하위 레이아웃을 더 중첩
5. 미리보기에서 위치·크기를 드래그로 조정
6. 필요 시 **구조 저장**으로 백업
7. **PNG 저장**

---

## 구조

```
index.html
tutorial.html / tutorial.css   # 사용자 튜토리얼
styles.css
.nojekyll                      # GitHub Pages용
js/version.js                  # 앱 릴리즈 버전 (MAJOR.MINOR.PATCH.BUILD)
js/grid.js                     # 격자 계산
js/i18n.js                     # UI 문자열
js/app.js                      # 편집기
icons/                         # favicon 등
```

---

## 라이선스

이 프로젝트는 **오픈소스가 아닙니다.**  
소스 공개(Source-Available)이며, 개인·비상업적 사용만 허용됩니다.

상업적 이용, 재배포, 타 서비스·플랫폼에의 통합·복제·이식은 [LICENSE](LICENSE)로 금지됩니다.  
상업 이용·제휴 문의: `khassarion@gamil.com`
